# 📖 คู่มือการใช้งาน NetConfig Tracer Studio v3 (User Guide)
### ฉบับจับมือทำ สำหรับผู้เริ่มต้นและบุคคลทั่วไป (Step-by-Step Complete Manual)

ยินดีต้อนรับสู่ **NetConfig Tracer Studio v3** ระบบบริหารจัดการและตั้งค่าอุปกรณ์เครือข่าย Cisco อัตโนมัติ (Network Automation) ผ่านหน้าต่างเว็บแอปพลิเคชันที่ใช้งานง่าย สวยงาม ทันสมัย และเห็นโครงสร้างเครือข่ายเสมือนจริง (Interactive Topology)

---

## 📑 สารบัญ (Table of Contents)
1. [สิ่งที่ต้องเตรียม และภาพรวมระบบ](#1-สิ่งที่ต้องเตรียม-และภาพรวมระบบ)
2. [การตั้งค่าอุปกรณ์ใน EVE-NG (สิ่งที่ต้องลงและคำสั่งเปิดสิทธิ์)](#2-การตั้งค่าอุปกรณ์ใน-eve-ng-สิ่งที่ต้องลงและคำสั่งเปิดสิทธิ์)
   - 2.1 [ชนิดอุปกรณ์ (Images/Templates) ที่แนะนำ](#21-ชนิดอุปกรณ์-imagestemplates-ที่แนะนำ)
   - 2.2 [สคริปต์เปิดสิทธิ์บน Router (Copy-Paste ได้ทันที)](#22-สคริปต์เปิดสิทธิ์บน-router-copy-paste-ได้ทันที)
   - 2.3 [สคริปต์เปิดสิทธิ์บน Switch (Copy-Paste ได้ทันที)](#23-สคริปต์เปิดสิทธิ์บน-switch-copy-paste-ได้ทันที)
   - 2.4 [การดูหมายเลขพอร์ต Telnet Console ใน EVE-NG](#24-การดูหมายเลขพอร์ต-telnet-console-ใน-eve-ng)
3. [วิธีเริ่มรันโปรแกรมบนคอมพิวเตอร์ของคุณ](#3-วิธีเริ่มรันโปรแกรมบนคอมพิวเตอร์ของคุณ)
4. [คู่มือการใช้งานหน้าจอหลักและแต่ละฟังก์ชัน](#4-คู่มือการใช้งานหน้าจอหลักและแต่ละฟังก์ชัน)
   - 4.1 [แถบด้านบน (Header & Quick Actions)](#41-แถบด้านบน-header--quick-actions)
   - 4.2 [เมนูด้านซ้าย: คลังอุปกรณ์ (Device Inventory)](#42-เมนูด้านซ้าย-คลังอุปกรณ์-device-inventory)
   - 4.3 [ผังเครือข่ายแบบโต้ตอบ (Interactive Topology Canvas)](#43-ผังเครือข่ายแบบโต้ตอบ-interactive-topology-canvas)
   - 4.4 [แท็บที่ 1: การตั้งค่า Interface (Interface Configuration)](#44-แท็บที่-1-การตั้งค่า-interface-interface-configuration)
   - 4.5 [แท็บที่ 2: การตั้งค่า Routing (Routing Protocols)](#45-แท็บที่-2-การตั้งค่า-routing-routing-protocols)
   - 4.6 [แท็บที่ 3: รวมคำสั่ง Show ยอดนิยม (Show Commands)](#46-แท็บที่-3-รวมคำสั่ง-show-ยอดนิยม-show-commands)
   - 4.7 [แท็บที่ 4: หน้าต่างพิมพ์คำสั่งจริง (Interactive CLI Terminal)](#47-แท็บที่-4-หน้าต่างพิมพ์คำสั่งจริง-interactive-cli-terminal)
   - 4.8 [แท็บที่ 5: จัดการการเชื่อมต่อ (Connection Manager)](#48-แท็บที่-5-จัดการการเชื่อมต่อ-connection-manager)
   - 4.9 [การจัดการ Virtual PC และการทดสอบยิง Ping](#49-การจัดการ-virtual-pc-และการทดสอบยิง-ping)
   - 4.10 [หน้าต่างดูพอร์ตไฟสถานะเสมือนจริง (Front-Panel Port Modal)](#410-หน้าต่างดูพอร์ตไฟสถานะเสมือนจริง-front-panel-port-modal)
   - 4.11 [การนำเข้าแล็บจาก EVE-NG อัตโนมัติ (EVE-NG Lab Import)](#411-การนำเข้าแล็บจาก-eve-ng-อัตโนมัติ-eve-ng-lab-import)
   - 4.12 [การส่งออกและกู้คืนคอนฟิก (Config Lifecycle)](#412-การส่งออกและกู้คืนคอนฟิก-config-lifecycle)
5. [คำถามที่พบบ่อยและการแก้ปัญหา (FAQ & Troubleshooting)](#5-คำถามที่พบบ่อยและการแก้ปัญหา-faq--troubleshooting)

---

## 1. สิ่งที่ต้องเตรียม และภาพรวมระบบ

โปรแกรมนี้จะทำงานเป็นสะพานเชื่อมระหว่าง **คุณ (ผ่านหน้าเว็บ)** กับ **อุปกรณ์ Cisco จริงหรือในแล็บจำลอง (EVE-NG / GNS3 / Physical Lab)**

```
┌─────────────────────────────────┐
│  ผู้ใช้งาน (Browser หน้าเว็บ)    │
│  http://127.0.0.1:5000          │
└────────────────┬────────────────┘
                 │ HTTP REST API
┌────────────────▼────────────────┐
│  NetConfig Tracer Studio v3     │
│  (Python + Flask + Netmiko)     │
└────────────────┬────────────────┘
                 │ Telnet / SSH / Serial
┌────────────────▼────────────────┐
│  อุปกรณ์ Cisco ใน EVE-NG / แล็บ  │
│  (Router R1, R2, R3, Switch S1) │
└─────────────────────────────────┘
```

---

## 2. การตั้งค่าอุปกรณ์ใน EVE-NG (สิ่งที่ต้องลงและคำสั่งเปิดสิทธิ์)

เพื่อให้โปรแกรมสามารถส่งคำสั่งเข้าไปปรับแต่งอุปกรณ์ Cisco ใน EVE-NG ได้อย่างราบรื่น อุปกรณ์จำเป็นต้องถูกเปิดสิทธิ์และตั้งค่าเบื้องต้นดังนี้

### 2.1 ชนิดอุปกรณ์ (Images/Templates) ที่แนะนำ
ใน EVE-NG คุณสามารถใช้อุปกรณ์ชนิดใดก็ได้ต่อไปนี้:
1. **Cisco IOL / IOU (แนะนำ)**: `i86bi-linux-l3-adventerprisek9` (Router) หรือ `i86bi-linux-l2-adventerprisek9` (Switch) — เบา กินแรมน้อย และเปิดเครื่องได้เร็ว
2. **Cisco Dynamips (c7200 / c3725)**: นิยมใช้ทั่วไป
3. **Cisco vIOS Router / vIOS-L2 Switch**: ใช้งานได้สมบูรณ์แบบ

---

### 2.2 สคริปต์เปิดสิทธิ์บน Router (Copy-Paste ได้ทันที)
เมื่อเปิด Node Router ใน EVE-NG ขึ้นมาเป็นครั้งแรก ให้กดเข้า Console ของ Router แล้วคัดลอกโค้ดด้านล่างนี้ไปวางได้ทันที:

```cisco
enable
configure terminal

! 1. ตั้งชื่อเครื่อง (เปลี่ยนเป็น R1, R2, R3 ตามต้องการ)
hostname R1

! 2. ตั้งรหัสผ่านระดับสูงสุด (Privilege 15) สำคัญมาก! หากไม่มีคำสั่งนี้ จะแก้คอนฟิกผ่าน Telnet ไม่ได้
enable secret cisco

! 3. เปิดสิทธิ์ให้เข้าควบคุมผ่าน Telnet ได้โดยไม่ติดรหัสผ่านขัดจังหวะ
line vty 0 4
 privilege level 15
 no login
 transport input all
 ! ปิดการตัดสายเมื่อไม่ได้พิมพ์อะไร (ป้องกัน Idle Timeout)
 exec-timeout 0 0
 logging synchronous
exit

! 4. ปิด timeout บนหน้า Console หลัก
line con 0
 privilege level 15
 exec-timeout 0 0
 logging synchronous
exit

! 5. บันทึกคอนฟิก
end
write memory
```

> [!IMPORTANT]
> **ทำไมต้องใส่ `enable secret cisco` และ `privilege level 15`?**  
> ตามระบบความปลอดภัยของ Cisco IOS หากไม่มีการตั้งค่า `enable secret` เอาไว้ อุปกรณ์จะ **ปฏิเสธ** การเข้าโหมดแก้ไขคอนฟิก (`configure terminal`) ผ่านสาย Telnet จากภายนอกทันที และคำสั่ง `exec-timeout 0 0` จะช่วยไม่ให้อุปกรณ์ตัดสายทิ้งเมื่อเราไม่ได้พิมพ์อะไรเป็นเวลานาน

---

### 2.3 สคริปต์เปิดสิทธิ์บน Switch (Copy-Paste ได้ทันที)
สำหรับ Switch (เช่น S1):

```cisco
enable
configure terminal

! 1. ตั้งชื่อเครื่อง
hostname S1

! 2. ตั้งรหัสผ่าน Enable
enable secret cisco

! 3. ตั้งค่า Line VTY ให้พร้อมรับคำสั่งระบบออโตเมชัน
line vty 0 15
 privilege level 15
 no login
 transport input all
 exec-timeout 0 0
 logging synchronous
exit

! 4. ปิด timeout บน Console
line con 0
 privilege level 15
 exec-timeout 0 0
 logging synchronous
exit

end
write memory
```

---

### 2.4 การดูหมายเลขพอร์ต Telnet Console ใน EVE-NG
บนหน้าต่าง Web EVE-NG:
1. เอาเมาส์ไปชี้ที่รูป Router หรือ Switch ที่กำลังรันอยู่ (ไฟสีฟ้า)
2. ดูที่มุมซ้ายล่างของหน้าต่างเบราว์เซอร์ จะเห็นลิงก์เช่น `telnet://192.168.74.131:32769`
3. ค่าที่นำมาใช้คือ:
   - **IP Address**: IP ของเครื่อง EVE-NG Server (เช่น `192.168.74.131`)
   - **Port**: หมายเลขพอร์ต 5 หลัก (เช่น `32769` สำหรับ R1, `32770` สำหรับ R2, `32771` สำหรับ R3)

---

## 3. วิธีเริ่มรันโปรแกรมบนคอมพิวเตอร์ของคุณ

1. เปิดโปรแกรม **Terminal / PowerShell** หรือ **Command Prompt**
2. ไปที่โฟลเดอร์ของโปรเจกต์:
   ```bash
   cd "C:\Users\phala\Downloads\มจพ\ปี 2 เทอม 1\Network Programming\Assignment 2"
   ```
3. ติดตั้งไลบรารีที่จำเป็น (ทำเพียงครั้งแรก):
   ```bash
   pip install -r requirements.txt
   ```
4. สั่งรันเว็บเซิร์ฟเวอร์:
   ```bash
   python app.py
   ```
5. เปิดเบราว์เซอร์ (Chrome / Edge / Firefox) แล้วพิมพ์:
   👉 **`http://127.0.0.1:5000`**

---

## 4. คู่มือการใช้งานหน้าจอหลักและแต่ละฟังก์ชัน

หน้าต่างโปรแกรมแบ่งออกเป็นสัดส่วนที่ชัดเจน ใช้งานง่าย ดังนี้:

![UI Architecture Overview](https://raw.githubusercontent.com/PuvadonKub/Assignment-2-NetPro/main/static/icons/router.svg) *(สัญลักษณ์ระบบอัตโนมัติ NetConfig)*

### 4.1 แถบด้านบน (Header & Quick Actions)
แถบด้านบนสุดคือศูนย์รวมสถานะและการสั่งการภาพรวม:
- **Active Device Dropdown**: เมนูเลือกอุปกรณ์หลักที่เรากำลังจะสั่งการ (เช่น เลือก `R1`) ทุกเมนูด้านล่างจะปรับมาสั่งการอุปกรณ์ตัวนี้ทันที
- **Auto-Discovery Button (ปุ่มแว่นขยาย)**: กดเพื่อให้ระบบสแกนตรวจสอบ IP บน Interface ทั้งหมด และคำนวณวาดเส้นเชื่อมโยงบนผัง Topology อัตโนมัติ
- **Hardware Profiles**: คลิกเพื่อดูสเปกฮาร์ดแวร์ โมเดลเราเตอร์ และจำนวนพอร์ตตามมาตรฐาน Cisco
- **EVE-NG Import**: คลิกเพื่อดึงรายชื่อโหนดและพอร์ตทั้งหมดจากแล็บใน EVE-NG เข้ามาในโปรแกรมได้ในคลิกเดียว
- **Config Lifecycle**: เมนูสำหรับ Export แบ็กอัปคอนฟิก (`running-config` / `startup-config`) ออกมาเป็นไฟล์ `.txt` หรืออัปโหลดคอนฟิกกลับเข้าไป

---

### 4.2 เมนูด้านซ้าย: คลังอุปกรณ์ (Device Inventory)
อยู่บริเวณแถบซ้ายมือของหน้าจอ:
- **รายการอุปกรณ์**: แสดง Router, Switch, และ PC ทั้งหมด
  - 🟢 **จุดสีเขียว**: เชื่อมต่อกับอุปกรณ์จริงเรียบร้อยแล้ว พร้อมส่งคำสั่ง
  - ⚪ **จุดสีเทา**: ยังไม่ได้เชื่อมต่อ
- **ปุ่ม Connect / Disconnect (รูปปลั๊กไฟ)**: คลิกเพื่อสั่งเชื่อมต่อไปยังเราเตอร์ตัวนั้นทันที
- **ปุ่ม Add Device (เครื่องหมาย +)**: หากต้องการเพิ่มอุปกรณ์ใหม่เข้าไปในระบบ ให้กรอก:
  - **Device Name / ID**: ชื่ออุปกรณ์ เช่น `R4` หรือ `S2`
  - **Type**: เลือกว่าเป็น `Router`, `Switch`, หรือ `Virtual PC`
  - **Connection Protocol**: เลือก `TELNET`, `SSH`, หรือ `SERIAL`
  - **IP Address & Port**: เช่น IP ของ EVE-NG และพอร์ต Console `32772`
  - **Username / Password**: หากตั้งไว้ (ถ้าไม่มีให้เว้นว่างได้)
- **ปุ่มลบ (รูปถังขยะ)**: ลบอุปกรณ์ที่ไม่ใช้ออกจากรายการ

---

### 4.3 ผังเครือข่ายแบบโต้ตอบ (Interactive Topology Canvas)
อยู่ตรงกลางของหน้าจอ แสดงแผนผังเครือข่ายแบบภาพเคลื่อนไหวสด:
- **การลากและจัดวาง**: สามารถใช้เมาส์คลิกลากโหนดเพื่อจัดตำแหน่งได้ตามใจชอบ ซูมเข้า-ออกด้วยลูกกลิ้งเมาส์
- **ชนิดของเส้นเชื่อมโยง**:
  - **เส้นตรงระหว่างโหนด**: เป็นการเชื่อมต่อแบบจุดต่อจุด (Point-to-Point) มีป้ายบอกชื่อพอร์ตทั้งสองฝั่ง เช่น `R1 (e0/0) ── R2 (e0/0)`
  - **ก้อนเมฆ (Cloud Network)**: ระบบจะสร้างก้อนเมฆอัตโนมัติหากมีพอร์ตมากกว่า 2 โหนดเชื่อมต่ออยู่ในวง Subnet เดียวกัน
- **การคลิกที่อุปกรณ์**: เมื่อคลิกที่รูป Router ใดๆ ในผัง ระบบจะเปิดแถบควบคุมของอุปกรณ์ตัวนั้นขึ้นมาทันที

---

### 4.4 แท็บที่ 1: การตั้งค่า Interface (Interface Configuration)
ใช้สำหรับการตั้งค่าพอร์ตและ IP Address ให้กับเราเตอร์:

#### วิธีใช้งานแบบ 3 ขั้นตอนง่ายๆ:
1. **ขั้นตอนที่ 1 (Select Port)**: ดูตาราง Interface ของเราเตอร์ที่เลือก คลิกเลือกพอร์ตที่ต้องการตั้งค่า (เช่น `Ethernet0/0`)
2. **ขั้นตอนที่ 2 (Choose Mode & Enter IP)**:
   - 🔵 **โหมด Static IP**: ติ๊กเลือกโหมดนี้ จากนั้นพิมพ์ IP Address (เช่น `192.168.1.1`) และ Subnet Mask (เช่น `255.255.255.0`)
   - 🟢 **โหมด DHCP Client**: ติ๊กเลือกโหมดนี้หากต้องการให้พอร์ตนี้ขอรับ IP จากภายนอกอัตโนมัติ (ระบบจะสร้างคำสั่ง `ip address dhcp` ให้)
   - 🔴 **โหมด No IP Address**: ติ๊กเลือกโหมดนี้หากต้องการถอน IP ออกจากพอร์ต (ระบบจะสร้างคำสั่ง `no ip address` ให้)
3. **ขั้นตอนที่ 3 (Set State & Deploy)**:
   - เลือกว่าจะเปิดพอร์ต (**UP** - `no shutdown`) หรือปิดพอร์ต (**DOWN** - `shutdown`)
   - ดูตัวอย่างคำสั่งในกล่อง **CLI Preview**
   - กดปุ่ม **"Deploy Interface Config"** ระบบจะส่งคำสั่งไปยังเราเตอร์จริงทันที และแสดงผลการทำงานพร้อมไฟสถานะ

---

### 4.5 แท็บที่ 2: การตั้งค่า Routing (Routing Protocols)
ใช้สำหรับตั้งค่าเส้นทางเครือข่าย เพื่อให้อุปกรณ์ต่างวงคุยกันได้ รองรับโปรโตคอลมาตรฐานครบครัน:

- **Static Route (เส้นทางแบบกำหนดเอง)**:
  - ระบุ Network ปลายทาง, Subnet Mask, และ Next-Hop IP (IP ของเราเตอร์ถัดไป)
  - รองรับการสร้าง Default Route (`0.0.0.0 0.0.0.0 <next-hop>`) เพียงแค่ติ๊กช่อง Default Route
- **RIP v2**:
  - ระบุ Network ที่ต้องการประกาศในวง RIP
  - ระบบจะใส่ `version 2` และ `no auto-summary` ให้โดยอัตโนมัติตามมาตรฐานสากล
- **EIGRP**:
  - กรอกหมายเลข Autonomous System (AS Number) เช่น `100`
  - กรอก Network และ Wildcard Mask (เช่น `192.168.1.0` และ `0.0.0.255`)
- **OSPF**:
  - กรอก Process ID (เช่น `1`) และ Router ID (เช่น `1.1.1.1`)
  - กรอก Network, Wildcard Mask, และ Area (เช่น Area `0`)
  - มีตัวเลือกเสริมสำหรับการทำ Route Redistribution และ Default Information Originate
- **BGP**:
  - กรอก Local AS Number
  - ระบุ Neighbor IP และ Remote AS ของฝั่งตรงข้าม
- **การส่งคำสั่ง**: ก่อนกดส่ง คุณสามารถอ่านคำสั่ง Cisco IOS ที่ระบบสร้างให้ในกล่อง Preview ได้ เมื่อมั่นใจแล้วให้กด **"Deploy Routing Config"**

---

### 4.6 แท็บที่ 3: รวมคำสั่ง Show ยอดนิยม (Show Commands)
ไม่ต้องจำคำสั่งยาวๆ อีกต่อไป เพียงแค่คลิกปุ่ม:
- **หมวดทั่วไป**:
  - `ip int brief`: ตรวจสอบสถานะพอร์ตและ IP ทั้งหมด
  - `int status`: ตรวจสอบสถานะการเชื่อมต่อสาย
  - `running-config`: ดูการตั้งค่าปัจจุบันทั้งหมดของเครื่อง
  - `version`: ดูรุ่นและเวอร์ชันของ Cisco IOS
  - `vlan`: ตรวจสอบฐานข้อมูล VLAN (สำหรับ Switch)
- **หมวด Routing & Neighbors**:
  - `ip route`: ดูตาราง Routing Table (ดูเส้นทาง C, S, O, D, B)
  - `ip protocols`: ตรวจสอบว่าเปิดโปรโตคอลเส้นทางใดอยู่บ้าง
  - `ospf neighbor` / `eigrp neighbors` / `bgp summary`: ตรวจสอบคู่เชื่อมต่อว่าจับคู่สำเร็จหรือไม่
- ผลลัพธ์จะปรากฏในกล่อง Terminal ด้านล่าง พร้อมปุ่ม **Copy** สำหรับคัดลอกข้อความไปทำรายงาน

---

### 4.7 แท็บที่ 4: หน้าต่างพิมพ์คำสั่งจริง (Interactive CLI Terminal)
หน้าจอ Console สีดำเสมือนนั่งอยู่หน้าเครื่องจริง:
- **คำสั่งจริง 100%**: ทุกข้อความที่คุณพิมพ์จะถูกส่งตรงไปยัง Shell ของ Router/Switch ตัวจริง
- **Live Prompt**: เครื่องหมาย Prompt ด้านหน้าจะเปลี่ยนตามสถานะจริง เช่น เมื่อพิมพ์ `conf t` จะเปลี่ยนเป็น `R1(config)#` ทันที
- **ฟังก์ชันช่วยเหลือสุดพิเศษ**:
  - พิมพ์ **`?`**: ระบบจะแสดงรายการคำสั่งทั้งหมดที่สามารถพิมพ์ได้ในขณะนั้น เหมือนใน Cisco IOS จริง
  - กดปุ่ม **`Tab`**: เติมคำสั่งอัตโนมัติ (เช่น พิมพ์ `sh ip in` แล้วกด Tab จะเติมเป็น `show ip interface brief`)
  - กดปุ่ม **ลูกศรขึ้น / ลง (Arrow Up/Down)**: ดึงประวัติคำสั่งเก่าที่เคยพิมพ์กลับมา
  - คำสั่งพิเศษ: พิมพ์ `clear` เพื่อล้างหน้าจอ Terminal

---

### 4.8 แท็บที่ 5: จัดการการเชื่อมต่อ (Connection Manager)
ใช้สำหรับตรวจสอบและเชื่อมต่อกับอุปกรณ์:
- เลือกอุปกรณ์ที่ต้องการเชื่อมต่อจากเมนู Dropdown
- เลือกโปรโตคอล:
  - **TELNET**: สำหรับ EVE-NG Console หรือพอร์ตคอนโซลจำลอง
  - **SSH**: สำหรับอุปกรณ์จริงที่มีการเปิดพอร์ต 22 และตั้งค่า SSH ไว้แล้ว
  - **SERIAL**: สำหรับสาย Console จริงที่ต่อเข้ากับพอร์ต USB / COM ของคอมพิวเตอร์
- กดปุ่ม **Connect** เพื่อเริ่มการเชื่อมต่อ ระบบมีเทคโนโลยี **Liveness Probe** คอยตรวจจับและต่อสายใหม่อัตโนมัติหากสายหลุด

---

### 4.9 การจัดการ Virtual PC และการทดสอบยิง Ping
ในระบบคุณสามารถสร้างโหนดจำลองตัวแทนเครื่องคอมพิวเตอร์ของผู้ใช้ (Virtual PC เช่น PC1, PC2) ได้:
1. คลิกที่รูปคอมพิวเตอร์ในผัง Topology หน้าต่าง Drawer ด้านข้างจะเปิดขึ้น
2. **ตั้งค่า IP ของ PC**: กรอก IP Address, Subnet Mask, และ Default Gateway (ชี้ไปยัง IP พอร์ตของเราเตอร์ที่เชื่อมต่ออยู่) แล้วกด Save
3. **ทดสอบยิง Ping จาก PC**:
   - กรอก Target IP (IP ปลายทางที่ต้องการทดสอบ เช่น `8.8.8.8` หรือ IP เราเตอร์อีกฝั่ง)
   - กดปุ่ม **"Ping"**
   - ระบบจะทดสอบส่งสัญญาณและแจ้งว่าสำเร็จ (`SUCCESS`) หรือล้มเหลว (`FAIL`) พร้อมสถิติการตอบสนอง

---

### 4.10 หน้าต่างดูพอร์ตไฟสถานะเสมือนจริง (Front-Panel Port Modal)
คลิกที่เมนู **"Front Panel"** หรือคลิกที่โหนดเพื่อดูรูปแผงหน้าปัดเครื่อง:
- แสดงช่องเสียบสายพอร์ตแลนเสมือนจริง (GigabitEthernet, FastEthernet, Serial)
- **ไฟสถานะ LED**:
  - 🟢 **สีเขียว**: พอร์ตกำลังทำงานปกติ (Status: UP)
  - 🔴 **สีแดง**: พอร์ตถูกสั่งปิด หรือไม่ได้เสียบสาย (Status: DOWN)
- เอาเมาส์ชี้เพื่อดูรายละเอียด IP และความเร็วของพอร์ตนั้นๆ

---

### 4.11 การนำเข้าแล็บจาก EVE-NG อัตโนมัติ (EVE-NG Lab Import)
หากคุณมีแล็บที่สร้างไว้อยู่แล้วใน EVE-NG ไม่จำเป็นต้องมานั่งกรอกพอร์ตทีละตัว:
1. กดปุ่ม **"EVE-NG Import"** ที่แถบเมนูด้านบน
2. กรอกข้อมูล:
   - **EVE-NG Host**: เช่น `http://192.168.74.131`
   - **Username / Password**: ค่าเริ่มต้นของ EVE-NG คือ `admin` / `eve`
3. กด **"Connect & Fetch Labs"** ระบบจะไปดึงรายชื่อแล็บทั้งหมดในเซิร์ฟเวอร์มาให้เลือก
4. เลือกไฟล์แล็บของคุณ แล้วกด **"Import Topology"**
5. ระบบจะดึง Nodes, สายที่เชื่อมกัน, และพอร์ต Telnet มาสร้างในเว็บให้ทันทีแบบอัตโนมัติ

---

### 4.12 การส่งออกและกู้คืนคอนฟิก (Config Lifecycle)
- **Export Running-Config**: ดาวน์โหลดการตั้งค่าปัจจุบันเก็บไว้เป็นไฟล์ `.txt` บนคอมพิวเตอร์ของคุณ
- **Export Startup-Config**: ดาวน์โหลดคอนฟิกเริ่มต้นของเครื่อง
- **Merge Config**: วางข้อความคำสั่ง Cisco IOS ลงในช่องข้อความ แล้วกด Merge เพื่อส่งคำสั่งทั้งชุดเข้าไปประมวลผลพร้อมกันในครั้งเดียว
- **Save Config (write memory)**: สั่งบันทึกคอนฟิกปัจจุบันลงหน่วยความจำถาวร (NVRAM) เพื่อให้เวลาเปิดเครื่องใหม่คอนฟิกไม่หาย

---

## 5. คำถามที่พบบ่อยและการแก้ปัญหา (FAQ & Troubleshooting)

### Q1: ทำไมพิมพ์คำสั่งใน CLI แล้วขึ้นว่า "Device not connected"?
**ตอบ**: 
1. ตรวจสอบว่าใน EVE-NG อุปกรณ์ตัวนั้นถูกเปิดเครื่องอยู่หรือไม่ (ไฟต้องเป็นสีฟ้า)
2. ไปที่แท็บ **Connection Manager** หรือคลิกปุ่มรูปปลั๊กไฟในเมนูด้านซ้ายเพื่อสั่ง Connect ก่อน
3. ตรวจสอบว่า IP และหมายเลขพอร์ตในหน้า Inventory ตรงกับพอร์ต Telnet ใน EVE-NG หรือไม่

---

### Q2: เชื่อมต่อ Telnet ได้ แต่พอส่งคำสั่งแล้วฟ้องว่าเข้า Enable Mode ไม่ได้?
**ตอบ**: 
อุปกรณ์ Cisco ยังไม่มีการตั้งรหัสผ่าน Enable ให้เปิดหน้า Console ของอุปกรณ์ใน EVE-NG แล้วพิมพ์:
```cisco
enable
conf t
enable secret cisco
end
write memory
```
จากนั้นลองสั่งงานผ่านหน้าเว็บอีกครั้ง

---

### Q3: ทำไมสั่งโชว์คำสั่งยาวๆ เช่น `show running-config` แล้วค้าง?
**ตอบ**: 
เกิดจากอุปกรณ์ติดหน้าจอแบ่งหน้า (`--More--`) ซึ่งใน NetConfig Tracer Studio v3 ได้รับการแก้ไขแล้วด้วยการส่ง `terminal length 0` และ `terminal width 512` โดยอัตโนมัติ หากยังพบปัญหา ให้พิมพ์คำสั่ง `terminal length 0` ในหน้าต่าง CLI Terminal 1 ครั้ง

---

### Q4: หน้าต่างผัง Topology เป็นสีดำว่างเปล่า ไม่ขึ้นรูปวงกลม?
**ตอบ**: 
โปรแกรมจำเป็นต้องดึงไฟล์ไลบรารีแสดงผลกราฟิก (Vis-Network) ผ่านอินเทอร์เน็ตในการเปิดใช้งานครั้งแรก โปรดตรวจสอบว่าคอมพิวเตอร์ของคุณเชื่อมต่ออินเทอร์เน็ตอยู่ แล้วกดรีเฟรชหน้าเบราว์เซอร์ (`Ctrl + F5`)

---

### Q5: พอร์ต Telnet เข้าไม่ได้ ฟ้องว่าพอร์ตถูกใช้งานอยู่ (Permission Denied / Port in use)?
**ตอบ**: 
EVE-NG อนุญาตให้มีโปรแกรมเชื่อมต่อเข้า Console ได้เพียง 1 โปรแกรมต่อ 1 อุปกรณ์ในเวลาเดียวกัน โปรดปิดโปรแกรมอื่น เช่น **PuTTY, SecureCRT, Tera Term หรือ MobaXterm** ที่เปิดพอร์ตนั้นค้างไว้ แล้วลองใหม่อีกครั้ง

---

## 6. แนวทางการทดสอบระบบตามเกณฑ์ Assignment 2 (Test Cases & Topology Blueprint)

สำหรับการทดสอบระบบอย่างเป็นระบบ (Systematic Verification) เพื่อเก็บคะแนน **10 คะแนนหลัก + 3 คะแนนโบนัส** โปรดดูรายละเอียดในเอกสาร:
👉 [TEST_CASES_CHECKLIST.md](file:///c:/Users/phala/Downloads/มจพ/ปี%202%20เทอม%201/Network%20Programming/Assignment%202/TEST_CASES_CHECKLIST.md)

ภายในเอกสารดังกล่าวมี:
1. **ผังเครือข่ายจำลอง (Simulated Topology Blueprint)**: โครงสร้าง Router 3 ตัว (R1, R2, R3), Switch (SW1), Linux PC (PC1), Virtual PC (PC2)
2. **ตารางจัดสรร IP & Protocol Matrix**: กำหนดค่าเชื่อมต่อครบทั้ง SSH, Telnet, Serial COM
3. **ขั้นตอนการทดสอบทีละข้อ (Step-by-Step Test Walkthrough)**: ครอบคลุม Interface IP/Up/Down, Routing 6 ชนิด (Static, Default, RIP, EIGRP, OSPF, BGP), Show Commands, Auto Discovery, และ Front Panel Ports
4. **ใบบันทึกคะแนน (Evaluation Scorecard)**: พร้อมให้พิมพ์หรือส่งอาจารย์ตรวจได้ทันที

---

**จัดทำขึ้นเพื่อการศึกษาและการเรียนรู้ Network Programming**  
*NetConfig Tracer Studio v3 — Automated Cisco Lab Solution*

